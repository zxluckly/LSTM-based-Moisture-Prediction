# 土壤墒情预测系统

基于若依框架和LSTM深度学习模型的智能土壤墒情预测系统

## 系统概述

本系统采用SpringBoot + Vue.js前后端分离架构，集成LSTM深度学习模型，实现土壤墒情的智能预测和监控。系统通过物联网设备采集土壤温度、湿度、电导率(EC值)、酸碱度(pH值)等关键参数，经过数据预处理和归一化后，利用LSTM模型进行时间序列预测，为农业生产提供科学决策支持。

## 技术架构

### 后端技术栈
- **框架**: SpringBoot 2.5.15
- **持久层**: MyBatis + MySQL
- **安全框架**: Spring Security + JWT
- **缓存**: Redis
- **API文档**: Swagger3
- **模型调用**: HTTP REST API (与Python LSTM微服务通信)

### 前端技术栈
- **框架**: Vue.js 2.x
- **UI组件**: Element UI
- **图表**: ECharts
- **构建工具**: Webpack

### AI模型服务
- **深度学习框架**: TensorFlow/Keras
- **模型类型**: LSTM (长短期记忆网络)
- **服务框架**: Flask
- **数据处理**: NumPy, Pandas, Scikit-learn

## 核心功能

### 1. 数据采集与管理
- **实时数据采集**: 支持多种物联网设备接入
- **数据质量控制**: 异常值检测、缺失值处理
- **数据预处理**: 数据清洗、归一化、时间序列组织
- **批量数据导入**: 支持CSV、JSON格式数据导入
- **模拟数据生成**: 内置数据生成器，便于测试和演示

### 2. LSTM预测模型
- **多时间尺度预测**:
  - 小时级预测: 基于7天历史数据，预测未来1-24小时
  - 天级预测: 基于30天历史数据，预测未来1-7天  
  - 周级预测: 基于90天历史数据，预测未来1-4周
- **多参数预测**: 同时预测温度、湿度、EC值、pH值
- **模型性能评估**: RMSE、MAE、准确度等指标
- **服务降级**: 当LSTM服务不可用时，自动切换到统计预测方法

### 3. 智能监控大屏
- **实时数据展示**: 多地块实时数据监控
- **趋势分析**: 历史数据趋势图表
- **预测结果可视化**: 预测值与实际值对比
- **地理信息展示**: 地块分布地图
- **预警系统**: 多级预警机制，支持多种通知方式

### 4. 系统管理
- **用户权限管理**: 基于角色的访问控制
- **设备管理**: 物联网设备注册、状态监控
- **地块管理**: 农田地块信息维护
- **预警规则配置**: 灵活的预警规则设置
- **系统监控**: 服务健康检查、性能监控

## 数据库设计

### 核心数据表
1. **soil_data**: 土壤数据表 - 存储传感器采集的原始数据
2. **soil_prediction**: 预测结果表 - 存储LSTM模型预测结果
3. **soil_field**: 地块信息表 - 农田地块基础信息
4. **soil_device**: 设备信息表 - 物联网设备管理
5. **soil_alert_rule**: 预警规则表 - 预警规则配置
6. **soil_alert_record**: 预警记录表 - 预警历史记录

## 部署说明

### 环境要求
- **Java**: JDK 1.8+
- **数据库**: MySQL 5.7+
- **缓存**: Redis 3.0+
- **Python**: Python 3.7+ (LSTM服务)
- **Node.js**: Node.js 12+ (前端构建)

### 部署步骤

#### 1. 数据库初始化
```sql
-- 1. 执行若依框架基础数据库脚本（如果是全新安装）
source sql/quartz.sql;
source sql/ry_20231130.sql;

-- 2. 执行土壤墒情系统数据库脚本
source sql/soil_moisture_system.sql;

-- 3. 单独执行菜单权限初始化（可选，如果上面脚本执行失败）
source sql/soil_menu_init.sql;
```

#### 2. 后端服务部署
```bash
# 编译打包
mvn clean package

# 启动服务
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

#### 3. LSTM微服务部署
```bash
cd python-lstm-service

# 安装依赖
pip install -r requirements.txt

# 启动服务
python app.py
```

#### 4. 前端部署
```bash
cd ruoyi-ui

# 安装依赖
npm install

# 开发环境启动
npm run dev

# 构建生产版本
npm run build:prod

# 部署到Web服务器
```

### 菜单权限配置

系统会自动创建以下菜单结构：

```
土壤墒情系统 (menu_id: 2000)
├── 土壤数据管理 (menu_id: 2001)
│   ├── 土壤数据查询 (soil:data:query)
│   ├── 土壤数据新增 (soil:data:add)
│   ├── 土壤数据修改 (soil:data:edit)
│   ├── 土壤数据删除 (soil:data:remove)
│   ├── 土壤数据导出 (soil:data:export)
│   └── 土壤数据导入 (soil:data:import)
├── 预测管理 (menu_id: 2002)
│   ├── 预测查询 (soil:prediction:query)
│   ├── 执行预测 (soil:prediction:predict)
│   ├── 预测修改 (soil:prediction:edit)
│   ├── 预测删除 (soil:prediction:remove)
│   └── 预测导出 (soil:prediction:export)
└── 监控大屏 (menu_id: 2003)
    └── 大屏查看 (soil:dashboard:view)
```

**注意事项：**
1. 菜单ID从2000开始，避免与系统原有菜单冲突
2. 默认给管理员角色分配所有权限
3. 如需给其他角色分配权限，请在系统管理-角色管理中配置

### 配置说明

#### 后端配置 (application-soil.yml)
```yaml
# LSTM服务配置
lstm:
  service:
    url: http://localhost:8080  # LSTM服务地址
    timeout: 30000             # 超时时间
    fallback-enabled: true     # 启用降级策略

# 数据采集配置
data:
  collection:
    interval: 60               # 采集间隔(分钟)
    batch-size: 1000          # 批处理大小
```

## API接口

### 土壤数据管理
- `GET /soil/data/list` - 查询土壤数据列表
- `POST /soil/data` - 新增土壤数据
- `PUT /soil/data` - 修改土壤数据
- `DELETE /soil/data/{ids}` - 删除土壤数据
- `POST /soil/data/simulate/{fieldId}/{count}` - 生成模拟数据
- `GET /soil/data/stats/realtime` - 获取实时统计

### 预测管理
- `POST /soil/prediction/predict/{fieldId}/{predictType}` - 执行预测
- `POST /soil/prediction/batch-predict` - 批量预测
- `GET /soil/prediction/model/performance` - 模型性能监控
- `GET /soil/prediction/trend/{fieldId}` - 预测趋势分析

### 监控大屏
- `GET /soil/dashboard/overview` - 总览数据
- `GET /soil/dashboard/realtime/{fieldId}` - 实时数据
- `GET /soil/dashboard/alerts` - 预警信息
- `GET /soil/dashboard/map` - 地图数据

## 使用指南

### 1. 数据采集
1. 在"设备管理"中注册物联网设备
2. 在"地块管理"中配置农田信息
3. 设备开始自动采集数据，或使用"生成模拟数据"功能

### 2. 预测分析
1. 进入"预测管理"页面
2. 选择地块和预测类型
3. 点击"执行预测"获得预测结果
4. 查看预测准确度和趋势分析

### 3. 监控大屏
1. 访问"监控大屏"页面
2. 实时查看各地块数据状态
3. 监控预警信息和系统健康状态
4. 分析历史趋势和预测结果

### 4. 预警配置
1. 在"预警规则"中设置阈值
2. 配置通知方式(系统/邮件/短信)
3. 系统自动监控并发送预警

## 系统特色

### 1. 智能预测
- 采用LSTM深度学习模型，能够捕捉土壤参数的长期依赖关系
- 支持多时间尺度预测，满足不同应用场景需求
- 提供预测准确度评估和模型性能监控

### 2. 数据质量保障
- 完整的数据预处理流程，包括异常值检测、缺失值处理
- 数据归一化处理，消除量纲影响
- 数据质量报告，实时监控数据状态

### 3. 高可用架构
- 微服务架构，LSTM模型独立部署
- 服务降级机制，确保系统稳定性
- 支持水平扩展和负载均衡

### 4. 用户友好
- 直观的监控大屏，实时展示关键指标
- 丰富的图表展示，支持趋势分析
- 灵活的预警配置，多种通知方式

## 扩展功能

### 1. 模型优化
- 支持模型在线训练和更新
- 多模型对比和A/B测试
- 自动超参数调优

### 2. 数据源扩展
- 支持更多类型的传感器
- 集成气象数据API
- 卫星遥感数据融合

### 3. 决策支持
- 灌溉建议算法
- 施肥方案推荐
- 病虫害预警

### 4. 移动端支持
- 微信小程序
- 移动APP
- 短信/邮件通知

## 技术优势

1. **先进的AI算法**: 采用LSTM深度学习模型，预测精度高
2. **完整的数据流程**: 从采集到预测的全链路数据处理
3. **灵活的架构设计**: 微服务架构，易于扩展和维护
4. **丰富的可视化**: 多维度数据展示和分析
5. **企业级安全**: 完善的权限控制和数据安全保障

## 联系方式

如有技术问题或合作需求，请联系开发团队。

---

*本系统基于若依开源框架开发，遵循Apache 2.0开源协议*